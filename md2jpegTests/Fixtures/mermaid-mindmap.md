## Product Discovery Map

```mermaid
mindmap
  root((md2jpeg))
    UX
      Preview
      Theme presets
      Export controls
    Rendering
      Markdown parser
      Mermaid runtime
      Snapshot export
    Quality
      Fixture tests
      Contrast checks
      Fallback diagnostics
```
